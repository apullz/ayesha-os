[package]
name = "ayesha-engine"
# Single-source of truth for the release version is ayesha.json ("version").
# Keep this in sync — the dist build and STATUS.md reference 4.5.0.
version = "4.5.0"
edition = "2021"
description = "terminal-native persona host with tool-calling for ollama"

[[bin]]
name = "ayesha-os"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
colored = "2"
base64 = "0.22"
crossterm = { version = "0.28", features = ["event-stream"] }
anyhow = "1"
dirs = "5"
image = "0.25"

[target.'cfg(windows)'.dependencies]
arboard = "3"

# arboard has no android clipboard backend (termux) — exclude it there; the
# usage sites are cfg-gated to graceful no-ops. still present on windows/linux/macos.
[target.'cfg(not(target_os = "android"))'.dependencies]
arboard = "3"

[build-dependencies]
